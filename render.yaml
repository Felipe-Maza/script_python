services:
  - type: cron
    name: sync-to-supabase
    schedule: "* * * * *"  # Cada minuto
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python migracion_datos.py"
    envVars:
      - fromFile: .env
